<%= form_for(@userauth) do |f| %>
  <% if @userauth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userauth.errors.count, "error") %> prohibited this userauth from being saved:</h2>

      <ul>
      <% @userauth.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
